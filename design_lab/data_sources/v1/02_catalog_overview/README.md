# 02 Catalog Overview

Purpose:

- keep the catalog tree structure from `01_catalog_tree`
- redesign the right pane around catalog-grain exploration
- adapt the Databricks object explorer shell into Stitchly's darker popup language

Included in this study:

- the same left-side catalog tree from the prior study
- breadcrumb-style catalog explorer header
- object title row with actions
- single active `Overview` tab
- overview-first content for grain, freshness, schema shape, and usage

What to review:

- whether the right pane now feels closer to a true catalog explorer
- whether a single-tab Overview surface is the right first grain for the browser
- whether the action row and breadcrumb treatment fit Stitchly's canvas popup language
- whether the overview sections give enough signal before deeper tabs exist
