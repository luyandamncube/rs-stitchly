# 05 SQL Editor

Purpose:

- build on the sample-data explorer from `04_sample_data`
- replace the table-header block with a lightweight SQL editor
- keep the result grid visible below the editor in the same right-side pane

Included in this study:

- the same left-side catalog tree shell
- `SQL Editor` label replacing the catalog breadcrumb
- compact SQL toolbar and two-line editor surface
- `Overview` and `Sample Data` tabs retained below the editor
- sample data grid reused as the result surface

What to review:

- whether the SQL editor feels like a natural next state for this explorer
- whether the toolbar is minimal but still believable enough
- whether the tabs still make sense once the editor is present
- whether the query area needs stronger contrast from the result grid
