# Integrations Lab · V1

This folder holds the first design pass for Stitchly integration popup surfaces.

Source inputs:

- the canvas menu / popup language already explored in `design_lab/menu/`
- the analytical popup structure explored in `design_lab/runs_history/`

Current studies:

- `00_integrations_window/`
  First pass for a canvas-triggered integrations popup with a compact connector table and a `+` connector tray.

Goals for this pass:

- keep the overall popup language close to `Runs & Logs`
- simplify the body into a table-first connection list
- test whether a small `+` dropdown can handle connector creation without adding extra chrome
