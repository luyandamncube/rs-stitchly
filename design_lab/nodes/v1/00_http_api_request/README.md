# 00 HTTP API Request

Type: `surface-study`

Purpose:

- create a first-node sample in the design lab
- closely follow the shared HTTP node reference structure
- keep Stitchly's dark `True Black + Lava Core` palette

What this sample is testing:

- outer shell proportions
- header icon/title/menu rhythm
- primary inset row treatment
- footer metric row treatment
- handle size and placement
- shared node-language CSS usage

Reference direction:

- the `HTTP API Request` node from the provided sample image set

Still unresolved:

- final icon language
- final typography choice for production
- exact row density for real data-rich nodes
- whether footer metrics should always use inset surfaces

Shared styling:

- this sample now uses `../shared.css`
- it should stay inside the shared node-language system unless a later review justifies a local override

Review questions:

- does the card feel structurally close enough to the sample?
- are the header and row proportions right?
- do the handles feel too loud or too quiet?
- is the dark adaptation still clean and premium?
